import 'package:flutter/material.dart';

class SuccessForeground extends StatelessWidget {
  const SuccessForeground({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: SingleChildScrollView(child: Column(children: [])),
    );
  }
}
